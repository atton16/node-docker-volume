{
  "name": "hello-docker-volume",
  "version": "1.0.0",
  "description": "```bash\r docker pull node:12-alpine\r ```",
  "main": "hello.js",
  "scripts": {
    "build": "echo \"Fake build successful.\" && exit 0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}

{
  "name": "node-docker-volume",
  "version": "1.0.0",
  "description": "An example of how to run node.js code stored on external volume on node base image",
  "main": "dist/index.js",
  "scripts": {
    "build": "mkdir -p dist && cp index.js dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
